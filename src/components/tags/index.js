import styled from "styled-components";

const Tags = () => {
  return <TagsWrapper>dsad</TagsWrapper>;
};

export default Tags;

const TagsWrapper = styled.div`
  background-color: #1e1d18;
  height: 220px;
`;
